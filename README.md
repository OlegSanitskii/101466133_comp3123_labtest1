Question 3

**node question03.js add**

Creates a **Logs** directory if it does not exist

Changes the current working directory to **Logs**

Creates 10 log files

Writes text into each file

Prints the file names to the console



**node question03.js remove**

Deletes all files inside the **Logs** directory (if it
exists), printing their names

Removes the **Logs** directory itself

Prints confirmation to the console
